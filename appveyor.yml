image: Visual Studio 2017
configuration : Release

environment:
  nodejs_version: "8"
  
before_build:
  - nuget restore

install:
  - ps: Install-Product node $env:nodejs_version
  - node --version
  - npm --version
  
build:
  verbosity: minimal
  publish_wap: true
  
